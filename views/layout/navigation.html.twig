<nav class="bg-blue-800">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
        <div class="flex h-16 items-center justify-between">
            <div class="flex items-center">
                <div class="flex-shrink-0">
                    <a href="/">
                        <img class="h-8 w-8" src="/images/logo.png" alt="Logo de Emeline Numérique" />
                    </a>
                </div>
                <div class="hidden md:block">
                    <div class="ml-10 flex items-baseline space-x-4">
                        <!-- Current: "bg-blue-900 text-white", Default: "text-gray-300 hover:bg-blue-700 hover:text-white" -->
                        <a href="/" class="{{ urlIs('/') ? 'bg-blue-900 text-white' : 'text-gray-300' }} hover:bg-blue-700 hover:text-white rounded-md px-3 py-2 text-sm font-medium" aria-current="page">Accueil</a>

                        <a href="/posts/list" class="{{ urlStartsWith('/posts') ? 'bg-blue-900 text-white' : 'text-gray-300' }} hover:bg-blue-700 hover:text-white rounded-md px-3 py-2 text-sm font-medium">Blogs posts</a>

                        {% if logged_in and user_role == 'admin' %}
                            <a href="#" class="{{ urlIs('#') ? 'bg-blue-900 text-white' : 'text-gray-300' }} hover:bg-blue-700 hover:text-white rounded-md px-3 py-2 text-sm font-medium">Utilisateurs</a>

                            <a href="/comments/validation" class="{{ urlIs('/comments/validation') ? 'bg-blue-900 text-white' : 'text-gray-300' }} hover:bg-blue-700 hover:text-white rounded-md px-3 py-2 text-sm font-medium"><span class="bg-blue-200 text-blue-900 text-xs px-1 rounded-full">{{ numberOfCommentsNotValidated() }}</span> Commentaires à valider</a>
                        {% endif %}
                    </div>
                </div>
            </div>

            <div class="md:block">
                <div class="ml-4 flex items-center md:ml-6">
                    <button type="button" class="rounded-full bg-blue-800 p-1 text-gray-400 hover:text-white focus:outline-none focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-blue-800">
                        <span class="sr-only">Connexion</span>
                        {% if logged_in == false %}
                            <a href="/users/login" class="text-red-500">{{ include('heroicons/power.svg') }}</a>
                        {% else %}
                            <a href="/users/logout"  class="text-green-500">{{ include('heroicons/power.svg') }}</a>
                        {% endif %}
                    </button>
                </div>
            </div>

            <div class="-mr-2 flex md:hidden">
            <!-- Mobile menu button -->
            <button type="button" id="nav-button" class="inline-flex items-center justify-center rounded-md bg-blue-800 p-2 text-gray-400 hover:bg-blue-700 hover:text-white focus:outline-none focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-gray-800" aria-controls="mobile-menu" aria-expanded="false">
                <span class="sr-only">Ouvrir le menu</span>
                <!-- Menu open: "hidden", Menu closed: "block" -->
                <svg class="nav-button block h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
                </svg>
                <!-- Menu open: "block", Menu closed: "hidden" -->
                <svg class="nav-button hidden h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
                </svg>
            </button>
        </div>
    </div>

    <!-- Mobile menu, show/hide based on menu state. -->
    <div class="md:hidden hidden" id="mobile-menu">
        <div class="space-y-1 px-2 pt-2 pb-3 sm:px-3">
            <!-- Current: "bg-blue-900 text-white", Default: "text-gray-300 hover:bg-blue-700 hover:text-white" -->
            <a href="/" class="{{ urlIs('/') ? 'bg-blue-900 text-white' : 'text-gray-300' }} hover:bg-blue-700 hover:text-white block rounded-md px-3 py-2 text-base font-medium" aria-current="page">Accueil</a>

            <a href="/posts/list" class="{{ urlStartsWith('/posts') ? 'bg-blue-900 text-white' : 'text-gray-300' }} hover:bg-blue-700 hover:text-white block rounded-md px-3 py-2 text-base font-medium">Blogs posts</a>

            {% if logged_in and user_role == 'admin' %}
                <a href="#" class="{{ urlIs('#') ? 'bg-blue-900 text-white' : 'text-gray-300' }} hover:bg-blue-700 hover:text-white block rounded-md px-3 py-2 text-base font-medium">Utilisateurs</a>

                <a href="/comments/validation" class="{{ urlIs('/comments/validation') ? 'bg-blue-900 text-white' : 'text-gray-300' }} hover:bg-blue-700 hover:text-white block rounded-md px-3 py-2 text-base font-medium"><span class="bg-blue-200 text-blue-900 text-xs px-1 rounded-full">{{ numberOfCommentsNotValidated() }}</span> Commentaires à valider</a>
            {% endif %}
        </div>
    </div>
</nav>